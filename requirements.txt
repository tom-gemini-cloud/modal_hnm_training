# Core ML and data processing
torch>=2.0.0
polars>=1.5.0
numpy>=1.21.0

# Progress bars for training
tqdm

# Modal deployment (optional - only needed for cloud training)
modal

# Model evaluation and analysis
pandas>=1.3.0
matplotlib>=3.5.0
seaborn>=0.11.0
scikit-learn>=1.0.0

# Jupyter notebook support
jupyter
ipykernel

# Development and optional dependencies
wandb  # Experiment tracking (Modal image also installs this)